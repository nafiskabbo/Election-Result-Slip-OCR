# Local API (optional). Render sets these in the dashboard.
# DATA_DIR=/data
# BALLOT_DB_PATH=/data/ballot_ocr.db
# STORAGE_DIR=/data/storage
PORT=8000
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
