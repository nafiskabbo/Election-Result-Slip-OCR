# Leave this unset for `npm run dev` so Vite proxies /api to localhost:8000.
# On Vercel, set this in Project → Settings → Environment Variables, then redeploy.
# No trailing slash.
# VITE_API_URL=https://your-service.onrender.com
