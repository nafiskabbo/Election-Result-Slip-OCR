# Leave this unset when the desk is served from the same origin as the API
# (local ./run.sh, or the VPS image). Set it only if the React app is hosted elsewhere.
# No trailing slash.
# VITE_API_URL=https://ballot.139.99.135.121.sslip.io
